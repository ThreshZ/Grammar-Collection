C语言中的指针运算符主要有两个：*和&

*是简接运算符，用于取出地址中存放的变量，如：

a=10;
b=&a;
c=*b;

所以：c=10
将地址b中存放的a取出，然后赋值给c，所以c=10。

&是取地址运算符，用于取某个值所在的地址，如：

a=10;
b=&a;

所以：b为某内存地址，例如b=fe38ax之类的。然后*b=10。

除此之外，*的另一个重要用途是用于指针声明，例如：int *p，char *p
